interface:
  display_name: "Orchestra Contract Validation"
  short_description: "templateとruntime contractをmaintainer向けに検証します"
  default_prompt: "$orchestra-contract-validation を明示的に使い、現在のtemplate contractを検証してください。"
policy:
  allow_implicit_invocation: false
